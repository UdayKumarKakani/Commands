 pipeline {
 	agent { docker {image 'node:22.4.1-alpine3.20'}}
 	stages {
 		stage('Build') {
 			steps {
 				echo "Build"
 			}
 		}
 		stage('Test') {
 			steps {
 				echo "Test"
 			}
 		}
 	}
 }
